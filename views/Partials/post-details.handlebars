<div class="card"> 
    <div class="card-header d-flex justify-content-between">
        <h2><a href="/post/{{id}}">{{title}}</a></h2>
        <p>Post by {{user.username}} on {{format_date dateCreated}}</p>
    </div>
    <div class="card-body"> 
        <p>{{content}}</p>
    </div> 
</div>