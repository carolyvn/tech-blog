<div>
    {{#each this}}
    <div class="card">
        <div class="class-header">
            <h2 clas="m-5"></h2>
        </div>
        <div class="card-body">
            <div>
                {{comment_text}}
            </div>
            <div>
                -{{user.username}}, {{format_date dateCreated}}
            </div>
        </div>
    </div>
    {{/each}}
</div>